<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bblu.wallet">

    <!-- ==================== PERMISSIONS ==================== -->
    
    <!-- Internet permission untuk koneksi ke Electrum server -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Camera permission untuk scan QR code -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Storage permission untuk backup wallet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    
    <!-- For Android 13+ (API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- For biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    
    <!-- ==================== FEATURES ==================== -->
    
    <!-- Camera feature untuk QR scanner -->
    <uses-feature 
        android:name="android.hardware.camera" 
        android:required="false" />
    <uses-feature 
        android:name="android.hardware.camera.autofocus" 
        android:required="false" />

    <!-- ==================== APPLICATION ==================== -->
    
    <application
        android:name=".BBLUWalletApplication"
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BBLUWallet"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- ==================== SPLASH SCREEN (Android 12+) ==================== -->
        
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BBLUWallet.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- ==================== MAIN ACTIVITIES ==================== -->
        
        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Send Activity -->
        <activity
            android:name=".ui.SendActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />
        
        <!-- Receive Activity -->
        <activity
            android:name=".ui.ReceiveActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />
        
        <!-- ==================== ADDITIONAL ACTIVITIES ==================== -->
        
        <!-- Create/Restore Wallet Activity -->
        <activity
            android:name=".ui.CreateWalletActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- Transaction Detail Activity -->
        <activity
            android:name=".ui.TransactionDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />
        
        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:screenOrientation="portrait" />
        
        <!-- About Activity -->
        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:parentActivityName=".ui.SettingsActivity"
            android:screenOrientation="portrait" />
        
        <!-- PIN/Password Activity -->
        <activity
            android:name=".ui.SecurityActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BBLUWallet" />
        
        <!-- QR Code Scanner Activity -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        
        <!-- ==================== SERVICES ==================== -->
        
        <!-- Wallet Sync Service (background sync dengan Electrum server) -->
        <service
            android:name=".services.WalletSyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- Notification Service -->
        <service
            android:name=".services.NotificationService"
            android:exported="false" />
        
        <!-- ==================== RECEIVERS ==================== -->
        
        <!-- Boot Receiver untuk restart sync service -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- Network Change Receiver -->
        <receiver
            android:name=".receivers.NetworkChangeReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        
        <!-- ==================== PROVIDERS ==================== -->
        
        <!-- File Provider untuk share file dan backup -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- ==================== METADATA ==================== -->
        
        <!-- Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        
        <!-- QR Code Scanner -->
        <meta-data
            android:name="com.google.zxing.client.android.DISABLE_AUTO_ORIENTATION"
            android:value="true" />
        
        <meta-data
            android:name="com.google.zxing.client.android.SCAN_PORTRAIT"
            android:value="true" />

    </application>

</manifest>
